<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- O atributo networkSecurityConfig vai aqui dentro da tag application -->
	<application android:allowBackup="true"
				 android:icon="@mipmap/appicon"
				 android:roundIcon="@mipmap/appicon_round"
				 android:supportsRtl="true"
				 android:networkSecurityConfig="@xml/network_security_config">  <!-- <<< MOVA PARA CÁ -->
	</application>

	<!-- As permissões ficam separadas -->
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<!-- Remova o atributo daqui -->

	<!-- Certifique-se de que essas permissões necessárias (INTERNET e ACCESS_NETWORK_STATE)
	     estejam presentes, o template padrão do MAUI geralmente já as inclui. -->

</manifest>